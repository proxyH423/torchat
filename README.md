# torchat
TorChat is a lightweight Bash-based peer-to-peer chat tool that uses cryptcat for encrypted communication and torsocks to route traffic securely over the Tor network. It allows users to connect using mapped .onion addresses stored in a local .env file. Designed for privacy-focused communication, TorChat supports both listen and connect modes.
